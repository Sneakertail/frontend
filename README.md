# frontend

Frontend service repo extracted from the Sneakertail monorepo.

## Contents

- `src/`, `public/`, `Dockerfile`, `nginx.conf`
- `.github/workflows/` for CI placeholders
- `k8s-manifests/` for direct Argo CD or `kubectl` usage
